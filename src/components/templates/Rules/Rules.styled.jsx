import styled from 'styled-components'

export const StyledComponents = styled.div`
    details {
        background-color: #ccc;
        border-radius: 8px;
        padding: 8px;
        margin: 12px 0;
        ul, ol {
            margin-top: 10px;
            padding-left: 18px;
        }
    }
`